# ตั้งค่าเมื่อจะใช้ Supabase (production). ถ้าไม่ตั้ง → รันบน in-memory seeded store (dev/preview)
USE_SUPABASE=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Auth (v1.2) — เซ็น session cookie (production ต้องตั้ง; ตั้งเป็นสตริงสุ่มยาว)
SESSION_SECRET=
